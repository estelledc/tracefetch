name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Set up uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.11.28"
          enable-cache: true
      - name: Install locked dependencies
        run: uv sync --extra dev --frozen
      - name: Run quality gates
        run: make check
      - name: Run installed product smoke
        run: |
          uv run --locked tracefetch --version
          uv run --locked tracefetch search "TraceFetch provider" --root . --limit 1 > /tmp/search.json
          python -c 'import json; assert json.load(open("/tmp/search.json"))["schema_version"] == "tracefetch.search-results.v1"'
          uv run --locked tracefetch doctor > /tmp/doctor.json
          python -c 'import json; assert json.load(open("/tmp/doctor.json"))["schema_version"] == "tracefetch.doctor.v1"'
